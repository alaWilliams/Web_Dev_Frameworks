// : Create a new React component that renders an unordered list of three different colors, each color as a string in list item. The colors are defined in the ColorList component and are always the same. Import the component and render it in the main App component



export default function Exercise2() {
  return (
    <ul>
      <li>Red</li>
      <li>Blue</li>
      <li>White</li>
    </ul>
  )
}
